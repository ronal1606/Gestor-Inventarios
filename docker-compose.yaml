services:
  app:
    build: .
    restart: always
    environment:
      APP_ENV: local
      APP_DEBUG: "false"
      APP_URL: https://programin.bid
      APP_KEY: ${APP_KEY}
      TRUSTED_PROXIES: "*"
      DB_HOST: db
      DB_CONNECTION: mysql
      DB_DATABASE: inventario
      DB_USERNAME: inventario
      DB_PASSWORD: inventario123
    volumes:
      # CAMBIO 1: Nombre nuevo aqui
      - web_root_final:/var/www/html
    depends_on:
      - db

  db:
    image: mysql:8.0
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment:
      MYSQL_DATABASE: inventario
      MYSQL_USER: inventario
      MYSQL_PASSWORD: inventario123
      MYSQL_ROOT_PASSWORD: root123
    volumes:
      - dbdata:/var/lib/mysql

  proxy:
    build:
      context: .
      dockerfile: nginx.Dockerfile
    restart: always
    ports:
      - "8052:80"
    expose:
      - "80"
    volumes:
      # CAMBIO 2: Nombre nuevo aqui tambien
      - web_root_final:/var/www/html
    depends_on:
      - app

volumes:
  # CAMBIO 3: Definir el volumen nuevo
  web_root_final:
  dbdata:
